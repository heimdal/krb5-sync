#!/bin/sh

set -e

# Restart krb5-admin-server on upgrade of the plugin to ensure it's using the
# latest version.
if invoke-rc.d krb5-admin-server status >/dev/null 2>&1 ; then
    invoke-rc.d krb5-admin-server restart
fi

#DEBHELPER#

exit 0
